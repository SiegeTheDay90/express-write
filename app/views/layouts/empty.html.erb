<!DOCTYPE html>
<html>
  <head>
    <title>ExpressWrite</title>

    <meta property="og:title" content="ExpressWrite - Cover Letters & Resumes" />
    <meta property="og:description" content="ExpressWrite is an AI project built by one developer. Use it for free!" />
    <meta property="og:url" content="https://express-write-6fb59feb3772.herokuapp.com/" />
    <meta property="og:image" content="https://cl-helper-development.s3.amazonaws.com/writewise-preview.png" />

    <meta value="Automatically Deployed!">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" in
    tegrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/a65848e44c.js" crossorigin="anonymous"></script>

    <link rel="icon" href="https://cl-helper-development.s3.amazonaws.com/logo.png" type="image/png">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_importmap_tags %>
  </head>

  <body id="main" class="splash">
    <%= yield %>

    <footer id="bug-report" class="d-flex flex-column">
      <span class="text-end"><%= link_to "Report a Bug", bug_report_url %> 🐞</span>
      <span><%= link_to "Stats for Nerds", stats_url %> 📊</span>
    </footer>
        <span id="alert-container">
        <h3>Notifications</h3>
        <% if flash["errors"] %>
          <% flash["errors"].each do |error|%>
            <meta class="balloon-message" data-type="error" data-text="<%= error %>">
          <% end %>
        <% end %>

        <%# <meta class="balloon-message" data-type="error" data-text="THIS IS A TEST.">
        <meta class="balloon-message" data-type="notice" data-text="THIS IS A TEST."> %>
        <% if flash["messages"] %>
          <% flash["messages"].each do |message|%>
            <meta class="balloon-message" data-type="notice" data-text="<%= message %>">
          <% end %>
        <% end %>

      </span>
  </body>
</html>
